import React from 'react';
import { Button } from '@chakra-ui/react';

export const FilterButton = (props) => <Button {...props}>Filter</Button>;
export const AddButton = (props) => <Button {...props}>Add New</Button>;
