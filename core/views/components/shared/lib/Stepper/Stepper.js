import React from 'react';
import { Box, Button } from '@chakra-ui/react';

export const VerticalStepper = ({ children }) => <Box>{children}</Box>;
export const VerticalStepperResetButton = (props) => <Button {...props}>Reset</Button>;
