// Stub: richText utilities for admin pages
const richTextToHtml = (content) => content || '';
const htmlToRichText = (html) => html || '';
const getConvertedStringJson = (json) => JSON.stringify(json);

module.exports = { richTextToHtml, htmlToRichText, getConvertedStringJson };
